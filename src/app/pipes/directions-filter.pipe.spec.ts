import { DirectionsFilterPipe } from './directions-filter.pipe';

describe('DirectionsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DirectionsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
